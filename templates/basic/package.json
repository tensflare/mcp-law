{
  "name": "basic-mcp-server",
  "version": "0.1.0",
  "description": "A basic MCP server scaffolded by MCP-Law — demonstrates the JSON-RPC over stdio pattern",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.0.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
